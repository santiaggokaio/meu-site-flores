import { createElement } from 'react';

export function ShoppingCart(props) {
  return createElement('svg', props, null);
}

// Se houver outros ícones, exporte-os aqui, por exemplo:
// export function Heart(props) {
//   return createElement('svg', props, null);
// }
